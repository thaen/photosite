{
  "private": true,
  "scripts": {
    "test:mariners": "playwright test tests/mariners.spec.mjs --reporter=line",
    "test:trivia": "playwright test tests/trivia.spec.mjs --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
